v1: Clicking a "hit me" button will render a new random fact

To Render:
"Hit Me" button
Trivia results

v2: Gameify
Components to make:
Button
Question block
Answer block
Score block


get array of all question objects
render question
render possible answers including correct answer
if user selects correct answer, assign score: value to be +1  
render score {state.score}/{state.questions.length}

On load should I just fill my state.question array with the questions and then do the rendering/logic with the button click?